#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../kissy-xtemplate/bin/xtemplate" "$@"
  ret=$?
else 
  node  "$basedir/../kissy-xtemplate/bin/xtemplate" "$@"
  ret=$?
fi
exit $ret
